{
  "name": "project-4",
  "version": "1.0.0",
  "description": "Safe entry",
  "main": "server/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server/server.js",
    "heroku-postbuild": "cd business_user && npm i && npm run build && cd ../public_user && npm i && npm run build && cd ../admin_user && npm i &&  npm run build",
    "dev": "nodemon server/server.js"
  },
  "keywords": [
    "COVID"
  ],
  "author": "Hilman",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "body-parser": "^1.19.0",
    "cookie-parser": "^1.4.5",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "joi": "^17.2.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.10.9"
  },
  "engines": {
    "node": "^14.0.0"
  }
}
